# `integration` 태그가 붙은 테스트는 실제로 떠 있는 백엔드가 필요하다.
# 서버 없이 `flutter test`를 돌려도 스위트가 깨지지 않도록, 해당 테스트는
# --dart-define=RUN_INTEGRATION=true 가 없으면 스스로 건너뛴다.
# (실행 방법은 test/integration/auth_flow_test.dart 상단 주석 참고)
tags:
  integration:
    skip: false
